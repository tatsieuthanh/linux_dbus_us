# Practice "Dbus|Unix socket|Process|Thread" in linux

## Overview
This project is a simple C application that prints "Hello, World!" to the console. It includes a `Makefile` to build the application.

## Project Structure